(function() {
  const { createElement, Fragment } = React;
  const { createRoot } = ReactDOM;
  /*매니저 창 중복되는부분 옮기고 지금 3개인거 하나로 통합예정*/ 

  function appmanage(){
    return createElement(
        'div',
        { className: 'appmanage' },

        createElement(
            
        )
    )
  }
})();

